class InvasionSensor is subclass of Sensor

--
-- Operations definition section
--
operations

public InvasionSensor: nat * Utils`Type * nat ==> InvasionSensor
	InvasionSensor (ID, t, val) ==
		(id := ID;
		type := t;
		value := val;
		);

public setValue: nat ==> ()
	setValue(v) ==
		value := v;

end InvasionSensor